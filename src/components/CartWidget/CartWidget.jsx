import { GiShoppingCart } from "react-icons/gi"

export const CartWidget = () => {
    return (
        <div>
            <GiShoppingCart size={20} />
            <span>0</span>
        </div>

    )
}
